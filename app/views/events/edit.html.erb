<div class="page-header">
  <h1><%= controller_name.humanize %><small><%= action_name %></small></h1>
</div>

<%= form_for [@banner] do |f| %>
    <%= render partial: 'form', locals:{f: f} %>
<% end %>
